# Project Tasks

Lightweight backlog for ideas captured during sessions. Surfaced at session start.
Mark complete by changing `[ ]` to `[x]`. Completed items can be deleted at will.

- [x] Create `private/` under `_inbox/` and `_outbox/` folders by default and verify they are gitignored
- [ ] Add webcrawler skill
